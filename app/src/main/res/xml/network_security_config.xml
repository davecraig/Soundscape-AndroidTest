<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- This is to allow HTTP access to local tile server whilst testing pmtiles generation -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
